sudo: required
services:
    - docker


before_install:
    - docker build -t ajiteshh/docker-react -f Dockerfile.dev .

script:
    - docker run ajiteshh/docker-react npm run test -- --coverage
    